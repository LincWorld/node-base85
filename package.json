{
  "name": "base85",
  "version": "3.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha test --timeout 20s && istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec"
  },
  "author": "brahma-dev",
  "license": "MIT",
  "devDependencies": {
    "assert": "^1.3.0",
    "coveralls": "^3.0.3",
    "istanbul": "^0.4.3",
    "mocha": "^6.1.4",
    "mocha-lcov-reporter": "^1.2.0",
    "should": "^13.2.3"
  },
  "engines": {
    "node": ">=10.8"
  }
}
